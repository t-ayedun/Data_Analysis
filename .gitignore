# ---------------------------------------------------------------
# Secrets - never commit. Credentials belong in Colab Secrets.
# ---------------------------------------------------------------
*.pem
*.key
*.ppk
.env
.env.*
!.env.example
secrets.json
credentials.json
service-account*.json

# ---------------------------------------------------------------
# Large data files - kept locally, not versioned
#   all_litres_data.csv                 ~13.8 MB
#   smarterise_commercial_client_data.csv ~11.3 MB
# ---------------------------------------------------------------
*.csv

# ---------------------------------------------------------------
# Original Google Drive zip exports (~22 MB, already extracted)
# ---------------------------------------------------------------
Archive/
*.zip

# ---------------------------------------------------------------
# A colleague's work - not ours to version or publish.
# Kept locally; excluded from the repo deliberately.
# ---------------------------------------------------------------
Data Analyst Capstone Project - Ibrahim Salman/

# ---------------------------------------------------------------
# Generated report artifacts - reproduced by running the notebook.
# "Monthly Report Grading + Report Visuals.ipynb" writes every month's
# outputs under reports/<REPORT_MONTH>/ (control-panel driven, see the
# notebook's first cell) - one rule covers all of it, all months.
# ---------------------------------------------------------------
reports/

# Still needed for the *other*, unmodified sibling notebooks that write a
# grades CSV straight to the repo root, e.g. Connect to SBEE Database.ipynb
*_grades.csv
reliability_metrics_*.png

# ---------------------------------------------------------------
# Python / Jupyter
# ---------------------------------------------------------------
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
.python-version

# ---------------------------------------------------------------
# OS / editor cruft
# ---------------------------------------------------------------
Thumbs.db
desktop.ini
.DS_Store
.vscode/

# Office lock files, left behind when a doc is open
~$*

# Google Drive / Colab sync leftovers
*.gdoc
*.gsheet
*.gslides
.tmp.driveupload/
.tmp.drivedownload/

# Colab "Untitled" scratch notebooks - noise, not work
Colab Notebooks/Untitled*.ipynb

# Notebook backups
*.bak

# Local secrets file read by the notebook's loader
.sbee_secrets.json
sbee_secrets.json
